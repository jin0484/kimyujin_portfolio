# 포트폴리오 사이트 스펙 — 김유진

## 컨셉
- 주제: **여백**
- 색: 배경 `--bg:#F3F3F3`, 잉크 `#0A0A0A`, 메인 컬러 `--main:#C3DCA8` 한 가지만
- 폰트: Pretendard(본문) + Archivo/Gothic A1(기타)

## 화면 1 — Figma "#1" 프레임 (1920×1080)

| 항목 | 내용 |
|---|---|
| 원본 | Figma 파일 김유진 › #1 (node 100:18), 호버 상태는 #2 (node 100:19). 좌표·크기는 Figma 값 그대로 `stage1.css` |
| 구조 | 폭 1920 무대 `#s1stage` 를 화면 폭에 맞춰 scale. 높이는 뷰포트에 맞춰 격자·본문이 늘어나고 KIM YUJIN 은 바닥(bottom) 기준으로 붙음 — 브라우저 탭·주소창 때문에 1080 이 안 돼도 가로가 꽉 참 |
| 요소 | 격자 SVG · 본문(Pretendard Medium 18/26, `--main`, 양끝맞춤, 왼쪽 2단 585px, Figma 메모: 자소서 내용·읽으라고 넣는 건 아님) · KIM YUJIN 글자별 SVG 8개 · 2026 PORTFOLIO SVG |
| 등장 | 처음 들어올 때 본문이 위에서 아래로 한 줄씩(clip-path steps, 줄당 38ms) |
| 본문 조판 | **Pretext**(`@chenglou/pretext`) — 폰트 로드 후 단어별 span 으로 바꾸고 `prepareWithSegments` 로 한 번 잰 뒤, 프레임마다 `layoutWithLines` 로 줄을 나눔(단어 단위 `keep-all`). 정렬(양끝/왼끝/오른끝)은 프레임의 `align`. 조판이 다른 프레임 사이에선 단어를 옮기지 않고 두 조판을 겹쳐 크로스페이드(Figma 스마트 애니메이트 방식) — 단어가 뒤섞이지 않음. 창·문단 위치·폭만 이징으로 이동 |
| 휠 시퀀스 | 첫 휠: KIM↔YUJIN 스왑(#2 배치, 0.7s)까지 가서 **멈춤**. 다음 휠: `화면1.pdf` 8프레임을 하나의 흐름으로 (`FRAMES`, stage1.js — `stop`이 정지점, `ms`가 구간 시간). 본문: 13줄로 짧아짐 → 3단·4단으로 길어지며 왼끝 맞춤으로 → 그대로 옆으로 이동 → 오른쪽 끝에 닿으며 오른끝 맞춤으로 → 2단으로 좁아져 오른끝 맞춤인 채 멈춤. 글자는 자기 영역 창 아래로 내려가 사라지고, 라벨은 위로, 노트북은 바닥까지. 휠 올리면 거꾸로(정지점에서 멈춤). 프레임 간 시간 `--s1step`, 이징 `--s1stepEase`. 호버 스왑은 제거(2026-09-22) |
| 노트북 | 6단, N 위 20px. 화면 안 `CLICK` 글자(Figma click 프레임 110:277)가 1.1s 주기로 깜빡임, 호버하면 켜진 채 멈춤. 클릭 → `work.html` |

## 제거된 것 (2026-09-22)
화면 2(신문 다단 조판 + 원형 파내기 + 프로젝트 모달), 화면 3(먼지 파티클 + 타이핑), 스크롤 오케스트레이션, 진행 레일, 안내 문구. 마지막 버전은 커밋 `714a960`.
