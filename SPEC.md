# 포트폴리오 사이트 스펙 — 김유진

`prototype.html` 이 파일 하나가 전체 동작하는 프로토타입이다. 이 문서는 그 안에서
**무엇이 의도된 설계이고, 무엇이 아직 임시인지**를 구분해 적은 것이다.

---

## 0. 한 줄 요약

스크롤 3단 구성. **글자로 꽉 찬 화면 → 신문처럼 정렬 → 먼지처럼 흩어지고 한 문장만 남음.**

---

## 1. 컨셉

- 주제: **여백**
- 스토리 키워드: 여백 / 경력 없는 신입 / 채워가는
- 최종 슬로건: `Keeping It Simple`
- 색: **흑백 + 포인트 컬러 1개** (`--main:#C3DCA8`). 한 화면에 컬러는 한 군데만 — 화면 1 방금 찍힌 글자, 화면 2 방금 판 구멍의 도형(또는 hover), 화면 3 캐럿. 배경은 항상 순백. (`--paper:#FFFFFF`, `--ink:#0A0A0A`)
- 폰트: Archivo(그로테스크) + Gothic A1(국문 본문) + 고운바탕(헤드라인 전용 명조)
- 격자: 작은 글씨는 항상 **단·행 격자(`--hair` 1px) 안에 갇혀** 있다. 화면 1 은 단마다 자기 사다리(세로 오프셋·문단 여백 반영), 화면 2 는 신문 다단 격자. 큰 글씨·구멍·캐럿은 격자를 무시한다. 화면 3 에서 글자가 먼지로 날아간 뒤에도 격자만 남았다가 마지막 문장이 타이핑되는 동안 사라진다. (`TUNE.gridAlpha`, `TUNE.gridWidth`)

---

## 2. 화면별 스펙

### 화면 1 — 꽉 참 (포인터 인터랙션)

| 항목 | 내용 |
|---|---|
| 기본 상태 | 작은 글씨가 화면을 빈틈없이 채움. 양끝맞춤, `word-break: break-all` |
| 큰 글씨 | 본문 흐름 안에 `<b class="w">`로 18개가 `font-size: 0.1px` 상태로 숨어 있음 |
| 인터랙션 | 마우스가 가까워지면 해당 글자가 최대 크기까지 자라며 **주변 텍스트를 실제로 밀어냄** |
| 왜 인라인인가 | `position:absolute`로 덮으면 재조판이 안 일어남. 레퍼런스의 핵심은 큰 글자와 작은 글자가 **같은 평면에서 자리를 뺏는 것** |
| 무한 루프 방지 | 초기화 시점에 "모든 글자가 0일 때의 좌표"를 한 번 측정해 고정. 거리 계산은 항상 그 고정 좌표 기준 (`measureAnchors()`) |
| 파라미터 | `MAXS` = 화면폭 11.5% (40~132px), `RAD` = 화면 긴 쪽 30% |
| 모바일 | `pointer: fine`이 아니면 큰 글씨가 리사주 곡선을 따라 자동 순회 (`autoTour`) |

### 화면 2 — 정렬 + 파내기

| 항목 | 내용 |
|---|---|
| 전환 | 화면 1이 블러되며 사라지고, 신문 요소가 위에서부터 85ms 간격 stagger로 내려옴 |
| 레이아웃 | 마스트헤드 / 헤드라인(명조) / 요약문 / 바이라인 / 5단 본문 (모바일 2단) |
| 헤드라인 | "단순히 예쁜 것보다 의미 있는 디자인을 하고 싶습니다" |
| 인터랙션 | **아무 데나 클릭 → 원이 하나씩 파임. 총 6개.** 위치는 `HOLES` 배열에 고정 (%) |
| 원 내용 | 더미 SVG 도형 + 프로젝트 이름 + `VIEW PROJECT →` |
| 도형 클릭 | 현재는 모달. **실제로는 프로젝트 상세 페이지로 라우팅되어야 함** |

### 화면 3 — 먼지 + 타이핑

| 항목 | 내용 |
|---|---|
| 구현 | `<canvas>` 파티클 최대 9,000개 |
| 소스 | `drawReplica()`가 화면 2와 비슷한 지면을 오프스크린 캔버스에 그리고, 픽셀을 샘플링해 파티클 생성 |
| 궤적 | 화면 중앙 기준 **회전 벡터(컬 필드) + 우상단 흐름**을 섞어 레퍼런스의 아치형 곡선을 만듦 |
| 중요 | **화면 2에서 파낸 원은 파티클 생성 시 제외됨** (`insideHole()`). 구멍을 3개만 파면 3개만큼 비어 있는 채로 흩어진다 |
| 마무리 | 진행도 88.5% 지점부터 `Keeping It Simple`이 62ms/글자로 타이핑. 위로 스크롤하면 리셋 |

---

## 3. 스크롤 구간 (`T` 객체)

전체 높이 `820vh`. 진행도 0~1 기준:

```
0.00 ─ 0.26   화면 1 고정 구간 (마우스로 놀 시간)
0.26 ─ 0.38   화면 1 → 화면 2 전환
0.38 ─ 0.66   화면 2 고정 구간 (클릭으로 파낼 시간)
0.66 ─ 0.70   화면 2 → 화면 3 전환
0.70 ─ 0.93   먼지 확산
0.885 ─ 1.00  타이핑
```

---

## 4. 반드시 교체해야 할 것 (현재 임시)

| 대상 | 현재 | 필요한 것 |
|---|---|---|
| **작은 글씨 전부** | `DUMMY` 상수의 더미 문장 | 실제 본문 (아직 미정) |
| **원 안의 오브젝트** | 더미 SVG 도형 6종 (`SHAPES`) | 프로젝트를 상징하는 실제 오브젝트 또는 썸네일 |
| **프로젝트 상세** | 모달 팝업 | 실제 상세 페이지 (라우팅) |
| **폰트** | Google Fonts | 최종 폰트 확정 후 교체 |

**큰 글씨 18개(`BIGS`)는 실제 내용이다.** 김유진에 대한 짧은 문장/단어라서 그대로 쓰면 된다.

---

## 5. 알려진 한계 / 개선 여지

1. **화면 2의 원은 텍스트를 진짜로 파낸 게 아니라 흰 원으로 덮은 것이다.**
   배경이 흰색이라 눈으로는 구분되지 않는다. 글자가 원을 피해 흐르게 하려면
   `shape-outside: circle()`을 5단 컬럼마다 쪼개서 적용해야 한다. 난이도 높음.

2. **화면 2 → 3 전환 시 DOM 텍스트와 canvas 복제본이 완전히 일치하지는 않는다.**
   빠른 크로스페이드 + 약간의 초기 확산으로 가려놓은 상태.

3. **화면 1에서 큰 글씨가 커지면 아래 텍스트가 화면 밖으로 밀려나 잘린다.**
   레퍼런스도 가장자리가 잘려 있어 의도로 읽히지만, 거슬리면 `MAXS`를 낮추면 된다.

4. **접근성.** 현재 키보드만으로는 원을 파낼 수 없고, 화면 1의 인터랙션도 포인터 전용이다.
   `prefers-reduced-motion` 감지 코드는 있으나 실제로 분기 처리되어 있지 않다.

5. **파티클 성능.** 고해상도 대화면에서 DPR을 1.5로 제한 중. 더 낮춰야 할 수 있다.

---

## 6. 레퍼런스

1. **화면 1** — 스위스 타이포 포스터. 거대한 글자가 음절 단위로 쪼개져 여러 줄에 걸쳐 흐르고, 작은 본문이 그 틈을 빈틈없이 파고드는 구조.
2. **화면 2** — 신문 지면(NYT 계열). 다단 양끝맞춤 본문 + 명조 헤드라인 + 본문 사이에 끼어든 오브젝트.
3. **화면 3** — 지면의 글자가 입자가 되어 아치를 그리며 쓸려 나가는 이미지.
